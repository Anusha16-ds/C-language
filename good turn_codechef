#include <stdio.h>
int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        int x, y;
        scanf("%d %d", &x, &y);
        int sum=x+y;
        if(sum>6)
        {
            printf("Yes\n");
        }
        else
        {
            printf("No\n");
        }
    }
}
